# Changelog

All notable changes to the E-AI-S (EAS AI Adoption Tracker) project are recorded here.

Format: `- YYYY-MM-DD (channel) — description (scope)`
Channels: `claude` · `copilot` · `commit` · `manual`
This changelog is **append-only**. Every task, regardless of origin, must add an entry under `## [Unreleased]` per `.github/copilot-instructions.md` §4.

---

## [Unreleased]

- 2026-04-29 (claude+codex) — **Data: Seed sector/unit/practice SPOC contacts from `Hierarchy.xlsx`.** New migration `sql/052_seed_spoc_data_from_hierarchy.sql` populates the canonical org-chart contact info: 13/13 sectors, 10/12 units (COE + Service Excellence are absent from the spreadsheet and intentionally untouched), and 6/6 EAS practice SPOC emails (BFSI, CES, EPCS, EPS, ERP Solutions, GRC). ADI practices in the xlsx have no SPOC email — left untouched. Migration 049 was a NULL-only backfill from existing user records; this is the canonical overwrite. Codex P1 caught that the email-change trigger only PROMOTES the new holder and never demotes the prior one — without a demotion pass, the old `sector_spoc` / `dept_spoc` would keep their role + scope + RLS access alongside the new SPOC. Migration mirrors `revoke_org_role` inline (can't call the RPC from a migration because it gates on `auth.uid()` which is NULL there): identifies stale holders whose email won't survive the seed, demotes to `contributor`, nulls anchors, deactivates `practice_spoc` rows, clears stale `*_spoc_email` pointers, and writes a `role_change_log` entry with `source='admin_assign'` (codex P1 caught that the obvious `migration_052_seed` source value would violate the existing CHECK constraint and abort the migration on the first stale row). Practice level intentionally does NOT demote because EAS is multi-SPOC by design — `practices.practice_spoc_email` is a primary-contact hint, the authoritative grants live in `practice_spoc`. Applied via Supabase MCP. (sql/052_seed_spoc_data_from_hierarchy.sql)

- 2026-04-29 (claude+codex) — **Feat: Leaderboard UI polish + hierarchy enrichment.** Two related issues addressed in one pass. (1) Org Rankings (Sector / Unit / Practice tabs in `js/org-leaderboard.js`) was rendering as a plain inline-styled `<table>` that did not match the rest of the dashboard. Rewritten to use the existing `.leaderboard-card` chrome + a new pill-style segmented `.leaderboard-tabs` group, with rank medals (🥇🥈🥉), practice-color left-borders (matching the `practiceColors` map at `src/pages/index.html:2318-2321`), and a hash-based palette for sector/unit names. Codex review caught two issues fixed before commit: (a) tabs originally used `role="tab"` + `aria-pressed` together, which is invalid ARIA — now `role="group"` + `aria-pressed` only (segmented buttons, not WAI-ARIA tabs, since we don't implement roving tabindex / arrow-key navigation / tabpanel linkage); (b) `escapeHtml()` was missing on `sector` / `department` / `practice` / `name` `innerHTML` injections in the Practice Rankings cards and Top Contributors table. (2) Practice Rankings cards and Top Contributors table did not show which Department / Unit and Organization (Sector) a practice or contributor belonged to. New SQL migration `sql/051_leaderboard_hierarchy.sql` drops+recreates `get_employee_leaderboard(text, text)` (originally from `sql/016`) and `get_practice_leaderboard(text)` (which existed only in the live DB — never previously committed; captured here from `pg_get_functiondef`) with two new columns each: `department_name`, `sector_name`. Hierarchy resolves via `tasks.practice → practices.name → practices.department_id → departments → sectors` (chosen over `users.department_id` per codex review — the `users` FK is null for legacy / flat-sector users, but `tasks.practice` is `NOT NULL REFERENCES practices(name)`). Joins applied **after** aggregation in a CTE so they cannot multiply task rows; tie-break for ambiguous practice-name matches uses `MIN(p.id)` deterministically via `DISTINCT ON`. Score formula in `get_practice_leaderboard` preserved exactly (`time_saved*0.4 + tasks*0.3 + efficiency*0.2 + quality*2`). Grants restricted to `authenticated` only — leaderboard data is not a public-signup helper. `js/db.js` maps the new columns to `department` / `sector` on the returned objects; `src/pages/index.html` injects a `<div class="leaderboard-breadcrumb">${sector} › ${department}</div>` line on each Practice Rankings card and adds two `<th>Org</th><th>Unit</th>` columns to the Top Contributors table (no fixed widths — relies on existing `overflow-x:auto` in `.table-card` for mobile). Cache busters → `v=20260429f`. (sql/051_leaderboard_hierarchy.sql, js/db.js, js/org-leaderboard.js, css/dashboard.css, src/pages/index.html, src/pages/admin.html)

- 2026-04-29 (claude+codex) — **Fix: Org Hierarchy edit modal hidden by `.hidden !important`.** Follow-up to the same-day review sweep — exposing `OrgTree` to `window` made the inline handlers fire, but the modal still didn't appear because `#org-edit-modal` was authored with `class="modal hidden"` and `.hidden { display: none !important; }` (defined in both `css/variables.css` and inline in `admin.html`). Setting `m.style.display = 'flex'` couldn't override the `!important` rule, so the buttons "worked" silently — modal remained invisible and admins saw no editable controls. Fix: `openModal` now strips the `hidden` class before applying inline display; `closeEditModal` reapplies it on exit. Cache buster → `v=20260429g`. (js/admin-org-tree.js, src/pages/admin.html)

- 2026-04-29 (claude+codex) — **Fix: Phase 4 review-action sweep.** Six items from the post-Phase-4 QA report. (1) `js/admin-org-tree.js`: admin Add Sector / Cancel / Save buttons in the Org Hierarchy page silently failed because `OrgTree` was declared with `const` in a classic non-module script — inline `onclick="OrgTree.foo()"` handlers run in a scope chain that only sees globals, so the binding was unreachable and threw `ReferenceError`. Edit / +Unit / +Practice already worked because they go through the existing `data-org-action` event delegation. Fix: append `window.OrgTree = OrgTree;` at the end of the IIFE. Cache buster → `v=20260429f`. (2) `sql/049_backfill_spoc_emails_from_users.sql`: idempotent backfill of `sectors.sector_spoc_email` / `sector_spoc_name` and `departments.unit_spoc_email` / `unit_spoc_name` from canonical `users.role='sector_spoc'/'dept_spoc'` rows. Touches a row when EITHER email OR name is missing (codex P2 — name-only gaps were skipped in the first draft); never overwrites populated values; emits NOTICE counts of remaining NULLs (expected for branches with no provisioned SPOC user). Currently backfills 2/13 sectors and 1/12 units — the rest stay NULL pending SPOC user provisioning. (3) `sql/050_revoke_anon_security_definer.sql`: defense-in-depth `REVOKE EXECUTE FROM PUBLIC, anon` on every `SECURITY DEFINER` function that has no anon caller (audit performed via `grep` across `js/`, `src/pages/`, `server/`, `supabase/`). Keeps anon access only for the signup trio (`signup_contributor`, `recover_incomplete_profile`, `complete_profile`). Includes a verification DO block that uses `has_function_privilege(oid, 'EXECUTE')` directly (codex P1 — passing `pg_get_function_arguments()` through `regprocedure` failed on functions with parameter defaults). (4) `scripts/cleanup_qa_phase14_data.sql` (NOT a migration — wrapped in `BEGIN; … ROLLBACK;` for review-then-commit): deletes the `qa.hr.1777423178386@ejada.com` user, its 3 task probes (`ac919efa`, `a75cc2fc`, `2d10025f` — operator note: probe ran 3 tasks, not 2), associated `submission_approvals` rows, synthetic `migration_orphans` 114/115; reverts `departments.unit_spoc_name` on EAS from 'QA SPOC EAS' to NULL (so 049's backfill repopulates it from `oibrahim@ejada.com`); reverts `sectors.brand_color` on ECC from `#bada55` to NULL; resets `users.profile_completed = false` for `test.orphan@ejada.com`. (5) `scripts/rotate-test-passwords.mjs`: Auth-Admin-API helper that rotates the seeded `test.*@ejada.com` accounts plus `omar.helal.1234@gmail.com` to base64url-encoded 24-byte passwords. Dry-run by default (`ROTATE=1` to apply). Paginates `auth.admin.listUsers` (codex P2 — single-page call would silently miss targets in tenants with >1000 users); collects per-user failures and exits non-zero on partial-failure (codex P2 — silent partial failure was masking weak-password persistence); refuses to print credentials in dry-run mode (codex P2 — operators were copying generated-but-not-applied passwords into secrets stores). (6) Verified `creds.txt` is already in `.gitignore` and never committed (`git log --all -- creds.txt` empty); verified Leaderboard tabs already render with both `role="tab"` AND `data-lb-tab` attribute (org-leaderboard.js:48) — both LOW + MED items resolved without code change. Three rounds of `codex exec review --uncommitted` cleared P1 + four P2s before commit. (sql/049, sql/050, scripts/cleanup_qa_phase14_data.sql, scripts/rotate-test-passwords.mjs, js/admin-org-tree.js, src/pages/admin.html)

- 2026-04-29 (claude+codex) — **Feat: Org-hierarchy Phase 4 (Polish).** `sql/044_phase4_complete_profile_and_brand.sql`: new `complete_profile(p_sector_id, p_department_id, p_practice)` RPC that derives the user_id from `auth.uid()` server-side (replaces the client-side `users.update().eq('id', profile.id)` in profile-completion-modal.js — fixes a codex-flagged trust gap where any script could dispatch `eas:profile-incomplete` with another user's id); validates the sector→department→practice chain with active-flag and same-sector enforcement; calls `sync_user_role_from_org` server-side. New `move_unit(p_unit_id, p_new_sector_id)` and `move_practice(p_practice_id, p_new_department_id)` SECURITY DEFINER RPCs that enforce both source AND destination scope server-side for sector_spoc / dept_spoc — drag-to-reparent goes through these instead of two RLS-checked UPDATEs. New `sectors.brand_color` column with `^#[0-9A-Fa-f]{6}$` CHECK constraint (idempotent ADD CONSTRAINT IF NOT EXISTS pattern). JS: `js/admin-org-tree.js` rewritten to use event delegation + data-attrs (replaces inline onclick — codex XSS hygiene), fixes the +Unit/+Practice arg-order bug (was passing parent_id as the row-id positional arg), adds an HTML5 drag-to-reparent UX backed by the new RPCs, brand-color picker on sector edit modal, defense-in-depth hex regex on render. `js/admin-orphans.js` (new): admin-only Migration Orphans review page that lists `migration_orphans` rows, lets the admin pick a sector, writes `sector_id` back to the source table, and removes the orphan row. `js/hierarchy.js`: cache key now includes `activeOnly` so active-only and all-rows fetches don't contaminate each other (codex finding); adds `moveUnit()` / `movePractice()` helpers; `upsertSector` accepts `brand_color`. `js/profile-completion-modal.js` switched to the `complete_profile` RPC. `js/org-drilldown.js`: inline `onclick`/`onkeypress` replaced with single delegated event listener + `data-nav-hash`; `accentColor` constrained to a hex regex before injection (codex CSS-injection hardening); sector tiles tinted from `sectors.brand_color`. Phase 4 also resolves the codex review of Phase 2-3 JS (HIGH: client-trusted `profile.id`, inline-onclick injection, +Unit/+Practice arg-order bug, cache-key contamination) and a follow-up codex pass on Phase 4 itself (HIGH: brand_color render needed regex guard; generic upsert path bypassed move_unit/move_practice — now detects parent change and routes through the move RPC). Cache busters → `v=20260429c`. New admin nav: `Migration Orphans`. (sql/044, js/hierarchy.js, js/admin-org-tree.js, js/admin-orphans.js, js/profile-completion-modal.js, js/org-drilldown.js, src/pages/admin.html)

- 2026-04-29 (claude+codex) — **Feat: Org-hierarchy Phase 3 (Drill-down + leaderboard tabs).** Builds on Phase 1 (#4) DB foundation and Phase 2 (#5) hierarchy module. New `js/org-drilldown.js` (`EAS_OrgDrilldown`): URL-driven drill-down (`#org` → 13 sector tiles, `#org/<sectorId>` → unit grid with breadcrumb, `#org/<sectorId>/<unitId>` → practice grid). Backed by `get_sector_summary` / `get_unit_summary` RPCs + `get_org_leaderboard('practice')` for per-practice tile stats. Empty-state copy for flat sectors and unit-without-practices. New `js/org-leaderboard.js` (`EAS_OrgLeaderboard`): 3-tab leaderboard (Sector / Unit / Practice) with `?lb=` URL state persistence, backed by the `get_org_leaderboard(p_level, p_quarter_id)` RPC. `src/pages/index.html`: new 'Org Overview' nav item + page panel hosting `#org-drilldown-root`; new 'Org Rankings' section in the Leaderboard page above the existing practice rankings hosting `#org-leaderboard-root`; page-router wires both modules; sector_spoc auto-deep-links to `#org/<their_sector_id>` (their de-facto dashboard); `_easApplyNavHash` recognises `#org/<sectorId>/<unitId>` paths. `sql/042_phase3_view_permissions.sql`: seeds `web.org` view-key for all 8 roles. `sql/043_phase3_security_hardening.sql`: retroactive codex review of Phase 1-3 caught 5 high/medium issues; a follow-up codex pass on the fix found 4 more. All resolved: privilege-escalation in `sync_user_role_from_org` (now allows only trigger context / admin / self via `pg_trigger_depth() > 0` + `IS DISTINCT FROM 'admin'`), missing identity verification in `signup_contributor` (now both anon + authenticated paths verify `p_email` matches `auth.users.email` for the supplied `p_auth_id`; idempotent re-call), recursive RLS subquery in `departments_dept_spoc_update` (replaced with `get_user_department_sector_id()` SECURITY DEFINER helper), `revoke_org_role` not clearing anchors / `*_spoc_email` rows so users got auto-re-promoted on next login (now nulls all anchors + clears matching `*_spoc_email` rows + sets `profile_completed = false`), wrong audit source on email-change-driven syncs (added `p_source` param; trigger passes `auto_promote_email_change`). Cache busters → `v=20260429b`. PR #6.

- 2026-04-29 (claude+codex) — **Feat: Org-hierarchy Phase 2 (Cascading signup, profile modal, admin tree, scoped RLS).** New `js/hierarchy.js` (`EAS_Hierarchy`): cached `fetchSectors` / `fetchDepartmentsBySector` / `fetchPracticesByDepartment` + `fetchOrgTree`; `attachCascade()` wires three `<select>`s into a sector → unit → practice cascade with explicit `N/A — sector has no units` / `N/A — unit has no practices` locks for flat sectors; `validateCascade()` returns `{ok, error, sectorId, departmentId, practice}`; admin upsert helpers (RLS enforces scope). New `js/profile-completion-modal.js` listens for `eas:profile-incomplete` (Phase-1 dispatched) and shows the cascade pre-filled. New `js/admin-org-tree.js` (`OrgTree`) renders sectors → units → practices with role-scoped action buttons (admin sees all, sector_spoc within sector, dept_spoc within unit) + edit modal supporting parent-dropdown reparenting + cascade-deactivate; inactive rows dimmed. `src/pages/signup.html` 2-level dept/practice form replaced with a 3-level cascade; `signup_contributor` RPC now passes `p_sector_id` and `p_department_id`. `src/pages/admin.html` gets a new 'Org Hierarchy' nav item + page panel. `sql/041_phase2_org_tree_rls.sql`: scoped self-service RLS — `sector_spoc` can edit their sector's units/practices, `dept_spoc` their unit's practices; `WITH CHECK` clauses prevent reparenting; `ENABLE RLS` on `departments` + `practices` (were not previously protected — `admin_write` was the only policy). Cache busters → `v=20260429a`. PR #5.

- 2026-04-28 (claude+codex) — **Feat: Org-hierarchy Phase 1 (Foundation).** Lands the database foundation and auth-layer wiring for org-wide AI adoption tracking — 13 sectors above the existing department/practice tree, a new `sector_spoc` role with fallback approval rights, sector-aware RLS, denormalised `sector_id` on every data table, and email-driven auto-promotion. Seven SQL migrations (`sql/033_sectors.sql` → `sql/039_org_rollups.sql`): 033 adds the `sectors` table + `sector_id` columns + `populate_sector_id()` trigger + cascade-deactivate + hierarchy-anchor CHECKs (NOT VALID); 034 adds the `sector_spoc` role, `get_user_sector_id()` helper, drops the open `submission_approvals_read_all_authenticated` SELECT policy and replaces it with role-scoped SELECT policies (admin/sector_spoc/dept_spoc/spoc/team_lead/executive/contributor/viewer), recreates `pending_approvals` view as `security_invoker=true` so RLS applies through the view; 035 seeds the 13 Ejada sectors, 9 ECC unit rows, 8 ADI practices, merges Service Excellence → SE; 036 backfills `sector_id` via the practice → department → sector chain, creates `migration_orphans` + `hierarchy_migration_log` audit tables, runs `VALIDATE CONSTRAINT` on every NOT VALID CHECK; 037 creates `approver_resolution` composite type, `resolve_approver(p_practice, p_department_id, p_sector_id)` cascade RPC (multi-SPOC preserved at practice level), `sync_user_role_from_org()` (auto-promote on email match, never demote), `revoke_org_role()` admin RPC, UPDATE triggers on the three `*_spoc_email` columns, and `role_change_log` audit table; 038 extends `signup_contributor` to 8 args (adds `p_sector_id`, `p_department_id`) and adds `copilot_users.department_id`; 039 adds `get_sector_summary()`, `get_unit_summary()`, `get_org_leaderboard()` rollup RPCs aggregating from denormalised `sector_id`. JS rewrites: `js/db.js` removes `determineApprovalRouting` (subsumed by `resolve_approver`), keeps the `<5h` task auto-approve as a client-side business rule, adds `escalation_level` + `sector_id` to the submission_approvals payload (§6.4 column mapping), adds `sector_spoc` branch to `fetchPendingApprovals` and a new `fetchSectorFallbackQueue` helper. `js/auth.js` calls `sync_user_role_from_org` post-login and dispatches `eas:profile-incomplete` CustomEvent when `users.profile_completed=false`. `js/approvals-modal.js` includes `sector_spoc` in `APPROVER_ROLES` and renders an `escalation_level` pill. Cache busters bumped to `?v=20260428a` on index/admin/employee-status. Plan: `docs/superpowers/plans/2026-04-28-org-hierarchy-phase-1.md`. Tested locally against `supabase start` (Postgres 17.6) — DB-side smoke tests verified resolve_approver cascade, auto-promote on email change with no-demote rule, role_change_log capture, RLS policy installation, rollup RPCs returning expected counts. (sql/033-039, js/db.js, js/auth.js, js/approvals-modal.js, src/pages/*.html, supabase/config.toml)

- 2026-04-28 (claude) — **Docs: org-hierarchy spec — fix 7 issues from second-opinion review (Claude + Codex).** Tightened `docs/superpowers/specs/2026-04-28-org-hierarchy-design.md` to resolve correctness gaps before implementation: (1) hierarchy-anchor CHECK constraints now use `NOT VALID` in migration 033 + `VALIDATE CONSTRAINT` in 036 after backfill; (2) `resolve_approver()` keeps its 3-arg signature (per Codex pushback — flat-sector / unit-only submissions have no practice to traverse), JS sources `department_id`/`sector_id` from the user profile; (3) explicit `escalation_level` → column mapping (practice/unit/sector → `spoc_id`, admin → `admin_id`) so the existing `pending_approvals` view works unchanged; (4) migration 034 drops the open `submission_approvals_read_all_authenticated` policy and replaces it with role-scoped SELECT policies; (5) `determineApprovalRouting()` is explicitly removed (the `<5h` auto-approval bypass stays in JS as a business rule); (6) `users.sector_id` and `departments.sector_id` use `ON DELETE RESTRICT` to honour the deactivate-don't-delete invariant; (7) `populate_sector_id()` trigger now canonicalizes from chain when `practice` or `department_id` is non-null, only honouring client-supplied `sector_id` for true sector-direct writes. BRD/HLD/CODE_ARCHITECTURE/IMPLEMENTATION_NOTES unchanged — pre-implementation spec edit only. (docs/superpowers/specs/2026-04-28-org-hierarchy-design.md)

- 2026-04-27 (claude) — **Fix: prevent duplicate task on rapid double-click of "Save Task".** `saveTask()` in `src/pages/index.html` was an `async` function with multiple `await` points (cap validation, `Phase8.submitWithApproval`) and the modal stayed open during the network insert, so a second click within ~1s entered `saveTask()` again in parallel and both calls reached the insert — producing a duplicate row. Added a module-level `_savingTask` re-entrancy flag (set after sync validation passes, cleared in `finally`) plus button disable + "Saving…" text for visual feedback. Sync validation early-returns leave the flag clear so users can correct errors and retry. `saveAccomplishment()` is structurally similar but closes its modal before the await, so the same race is much less reachable there — left untouched. No cache-buster bump (the fix lives in inline JS in `index.html`; users need a hard reload). (src/pages/index.html)

- 2026-04-26 (claude) — **Feat: Weekly task hour caps.** Admins can configure two submission limits via a new "Task Hour Caps" page in `admin.html` → Configuration: `max_weekly_task_hours_before_ai` (default 60h) caps an employee's total "Time Without AI" across all tasks in a calendar week; `max_task_pct_after_ai` (default 30%) caps "Time With AI" as a percentage of "Time Without AI" per task. Limits are enforced on new task submissions only — existing tasks are exempt. New migration `sql/032_app_settings.sql` creates the `app_settings` k/v table (RLS: all-read, admin-write). New functions added to `js/db.js`: `getAppSettings()` (session-cached), `saveAppSettings()`, `fetchWeeklyHoursByEmployee()`. Validator `validateTaskCaps()` added to `src/pages/index.html` and called inside `saveTask()` before `Phase8.submitWithApproval`. Cache-bust bumped to `?v=20260426a` on `js/db.js` references. (sql/032_app_settings.sql, js/db.js, src/pages/index.html, src/pages/admin.html)

- 2026-04-25 (claude) — **Feat: Activity Log admin tab.** New "Activity Log" sidebar tab in `admin.html` (`data-page="activity-log"`) gives admins a paginated, filterable audit view of the `activity_log` table. Filter by date range and action type; page size 50 with Prev/Next navigation; "View" button opens a modal with pretty-printed JSON for each entry's details payload; Export CSV downloads the current page. New `fetchActivityLog()` function added to `js/db.js` (uses `select('*',{count:'exact'})` + `.range()` for server-side pagination, exposed on `EAS_DB`). Auth is enforced at the page level (admin.html is admin-only) and at the DB level via the existing `activity_admin_read` RLS policy — no new SQL needed. (src/pages/admin.html, js/db.js)

- 2026-04-24 (claude) — **Fix: IDE Usage scope + searchable team-lead assignment.** Contributors on the IDE Usage Stats page now see only their own row (client-side email match against `EAS_Auth.getUserProfile()`) and the "Summary by Practice" tab is hidden for them since a single-row rollup is noise. SPOC's "Assign Team Lead" modal replaces the plain `<select>` with a datalist-backed search input (`#tla-team-lead-search`, syncs a hidden `<select>` for the save path) and adds a `#tla-member-search` box above the member grid that live-filters the cards by name or email — selections are preserved while searching. (src/pages/index.html)

- 2026-04-24 (claude) — **Feat: Pending-approvals pop-up on login.** Approvers (`admin`, `spoc`, `dept_spoc`, `team_lead`) now see a post-login modal listing the top 5 submissions awaiting their review when their queue is non-empty, with the full count and a "Review approvals →" CTA to `index.html#approvals`. Dismiss-for-today button stores a per-user per-date flag in `localStorage` (`eas_approvals_popup_dismissed_<userId>_<YYYY-MM-DD>`) so it re-fires on subsequent calendar days only. Reuses the upcoming-events modal shell (`.events-modal-backdrop`) and extends `css/events.css` with row/pill styles. New module `js/approvals-modal.js` (IIFE `ApprovalsModal.openForCurrentUser`) calls `EAS_DB.fetchPendingApprovals(role, practice, userId)` — no new SQL. Wired into `js/auth.js` after the events-modal trigger and loaded from `src/pages/{index,admin,employee-status}.html`. (js/approvals-modal.js, js/auth.js, css/events.css, src/pages/*.html)

- 2026-04-24 (claude) — **Feat: Upcoming Events feature.** Admins can publish events (AI sessions, summits, certifications, workshops, webinars) surfaced as a post-login pop-up to all authenticated users with hybrid RSVP tracking (internal `event_registrations` row + optional redirect to external registration URL). New migration `sql/031_events.sql` adds `events`, `event_registrations`, `event_dismissals`, a `v_active_events_for_user` RLS-scoped view, and 12 policies (public-read-active, admin-only writes, user-own RSVP/dismissal). New modules `js/events-modal.js` (pop-up + header bell + badge) and `js/admin-events.js` (Upcoming Events tab in admin — list/create/edit/delete, per-event registrations view with CSV export). `js/auth.js` auto-opens the modal once per page load after successful auth. HTML wiring in `src/pages/index.html`, `src/pages/admin.html`, `src/pages/employee-status.html`. Seed event: Microsoft AI Learning Summit (May 5-6, 2026). Spec: `docs/superpowers/specs/2026-04-24-upcoming-events-design.md` (sql/031, js/events-modal.js, js/admin-events.js, js/auth.js, css/events.css, src/pages/*.html)

- 2026-04-23 (claude) — **Fix: move SPOC pending-approvals CSV export to the user-facing Approvals page.** The previous commit added the button to `admin.html` only, which is the admin console; SPOCs / team-leads / dept-SPOCs use the Approvals page on `index.html` (`#page-approvals`). Added a dedicated "Export Pending CSV" button to the page-header of `index.html` next to the existing (full-page) Export button, wired to the same `exportPendingApprovalsCsv()` implementation (RLS-scoped, 40-col CSV with task + accomplishment details). Button is visible to admin/spoc/dept_spoc/team_lead via existing nav-level data-role gating. (src/pages/index.html)

- 2026-04-23 (claude) — **Feat: SPOC export of pending approvals to CSV.** Added "⬇ Export CSV" button in the Pending Approvals table header on the admin approvals view. Exports all `spoc_review` rows visible to the current user (RLS-scoped — SPOCs see only their practice, team leads see only assigned members, dept SPOCs see their department) with full details: submission type, practice, submitter, submitted-at + days pending, saved hours, and for tasks (project, week, description, category, AI tool, prompt, time-without/with/saved, quality, status, notes) or accomplishments (date, title, details, employees, before/after, impact, business gains, cost, effort saved, evidence, notes). CSV is UTF-8 BOM + CRLF, filename `pending_approvals_YYYY-MM-DD.csv`. (src/pages/admin.html)

- 2026-04-23 (claude) — **Fix: orphan spoc_review / admin_review rows polluting the SPOC queue.** When a contributor edits a task down under 5h (auto-approve threshold) or the client's delete-prior-approval step is blocked by RLS / bypassed (bulk sync, alternate path), the original `spoc_review` row in `submission_approvals` survived even though `tasks.approval_status='approved'` and `tasks.approval_id IS NULL`. Added migration `sql/030_supersede_orphan_approvals.sql`: (1) one-shot cleanup of 15 existing orphans, (2) BEFORE-INSERT trigger on `submission_approvals` that auto-supersedes any prior non-terminal row for the same submission, (3) AFTER-UPDATE triggers on `tasks` and `accomplishments` that supersede any lingering approval row whose id no longer matches `approval_id` or whose parent is already `approved`. Guarantees at most one active approval per submission going forward, regardless of client path. (sql/030)

- 2026-04-22 (claude) — **IDE Usage redesign**: new `ide_usage_daily` table (UPSERT on user_login+day) stores raw per-user per-day Copilot activity incl. JSONB breakdowns (IDE/feature/language/model); new rollup view + `refresh_copilot_users_ide_aggregates()` function keeps legacy `copilot_users.ide_*` columns in sync; RLS scopes visibility per role (admin/executive all · dept_spoc department · spoc practice · team_lead members · contributor own row · viewer denied); added `copilot_users.grafana_login` / `emp_id` / `unit`; redesigned `#page-ide-usage` with 3 tabs (Summary-by-Practice pivot · Excel-style All-Users roster with Practice/Unit/EmpID/Name/Email/Role/Login/Active/Days/Gen/Accept/LOC · per-user Drill-down with daily sparkline + IDE/feature/language/model breakdowns); new **Admin → IDE Usage Sync** page replaces the legacy Python Grafana script with a client-side NDJSON upload (chunked UPSERT + auto aggregate-refresh + unmatched-logins report) (sql/029, js/db.js, src/pages/index.html, src/pages/admin.html, css/dashboard.css)

- 2026-04-21 (claude) — Full offline database backup: 6 SQL files (schema, indexes, constraints, functions, triggers, RLS) + 24 JSON data exports (~1,847 rows) saved to backup/ directory with restore README (backup)

- 2026-04-21 (claude) — Enhanced Executive Summary dashboard: fixed weekly trend chart crash (null week_number + broken dual-axis), expanded to 6 primary KPIs (adoption rate, hrs/resource), added department drill-down with expand/collapse, secondary detailed metrics section, cumulative growth line on weekly trend, updated PDF/PPTX exports
- 2026-04-21 (claude) — Sync IDE stats from Grafana dump (2026-03-24 to 2026-04-20): 415 usernames processed, 40 copilot_users rows updated via batch SQL UPDATEs (scripts/sync_grafana_json.py)

- 2026-04-21 (claude) — Fix: dept_spoc RLS INSERT policies missing on tasks, accomplishments, submission_approvals — task submission failed with row-level security violation (sql/028)

- 2026-04-20 (claude) — add dept_spoc role with My Department dashboard, practice drill-down, and admin assignment UI (multi-department)

- 2026-04-20 (claude) — Fix: orphaned spoc_review approval records caused by double-click edits; updateTask/updateAccomplishment now purge ALL pending approval records for the submission before creating a new one, preventing ghost items in the SPOC queue; cleaned up existing orphans from DB (js/db.js)

- 2026-04-20 (claude) — Replace magic link button with admin-set password action; new Edge Function admin-reset-password (admin panel)

- 2026-04-20 (claude) — restrict Tasks, Licensed AI Users, and Projects views to own practice for SPOC; restrict Tasks and Licensed AI Users to assigned team members for Team Lead; Projects scoped to practice for Team Lead (index.html)

- 2026-04-19 (claude) — **Fix: 3 bugs — task date display, GH Copilot active status, My Practice inactive badge** — (1) Task Log / My Tasks / Practice modal: replaced week-number column with formatted `weekStart` date (e.g. "13 Apr 26"); updated all 3 "Week" headers to "Date". (2) `fetchCopilotUsers` now derives `githubCopilotStatus` from `ide_days_active > 0` — the `github_copilot_status` DB column defaults to `'inactive'` and is never written, so Grafana telemetry is the correct signal. (3) My Practice team table "Active/Inactive" badge now uses `fetchInactiveMembers` result (which scans ALL tasks by `created_at`, not just approved ones) for copilot-licensed users; only non-licensed users fall back to the leaderboard's approved `last_task_date`. (fix/ui+db)

- 2026-04-19 (claude) — **Fix/UX: 5 table UI improvements — Task Log columns, Licensed AI Users cleanup, IDE usage descriptions** — (1) Task Log thead was missing `<tr>` and the Practice+Week columns, causing all column headers to be shifted 2 positions left vs the rendered data rows; added both columns and fixed the tag. (2) Removed M365 Copilot column and KPI card from Licensed AI Users view (data retained in DB). (3) GH Copilot column in Licensed AI Users now shows inline Grafana IDE stats (days active, code gens, acceptances) sourced from `copilot_users.ide_*` fields, surfaced via `fetchCopilotUsers` mapping; column header has tooltip description. (4) "Has Logged" column header in Licensed AI Users now has tooltip explaining the adoption-rate formula. (5) All 13 column headers in the IDE Usage Stats admin view now have `title` tooltip descriptions explaining each Grafana metric. (fix/ui)

- 2026-04-19 (claude) — **Fix: 3 bugs — SPOC project duplication, log-task checkbox for non-admins, signup Grafana stats** — (1) `editProject()` was setting `fp-id` before calling `openModal('project')`, which reset the field to `''`, causing every SPOC/admin edit to INSERT a duplicate instead of UPDATE. Fixed by calling `openModal` first then populating fields. (2) Bypass-approval checkbox in the Log Task modal was always visible as a floating unchecked box for non-admins because only the span label was hidden. Fixed by wrapping both checkbox+label in `id="f-bypass-wrapper"` with `display:none` default, shown only for admins. (3) `signup_contributor` RPC did a plain INSERT into `copilot_users`; if a row already existed from Grafana sync, the unique-email constraint would fail and stats were lost. Replaced with `ON CONFLICT (email) DO UPDATE` to update profile fields while preserving all `ide_*` stat columns. Migration `024_signup_contributor_upsert_grafana_stats` applied. (fix/ui+db+auth)

- 2026-04-19 (claude) — **Fix: 11 bugs from manual test run BUG-01 through BUG-11** — BUG-01: Modal forms (task/accomplishment/project) now reset all fields on every fresh open. BUG-02: `saveProject` coerces empty `contract_value` to `null` instead of `0`. BUG-03: `saveTask` validates quality rating 1–5 in JS before save. BUG-04: Employee autocomplete now shows dropdown immediately after practice selection; duplicate listener accumulation eliminated. BUG-05: `updateTask`/`updateAccomplishment` handle RLS-blocked deletes by marking old approval rows `superseded`. BUG-06: `submitTaskWithApproval`/`submitAccomplishmentWithApproval` sync `approval_status` back to submission row on create. BUG-07: Bypass-approval toast reads "auto-approved (admin bypass)". BUG-08: Removed duplicate "Saving task…" toast — one toast per task save. BUG-09: Removed extra "Saving accomplishment…" and trailing toast — one toast per accomplishment save. BUG-10: `rejectSubmission` stamps `spoc_reviewed_by/name/at` when rejecting at `spoc_review` stage. BUG-11: Added `#user-display-practice` sidebar element; admin now sees home practice label. (fix/ui+db+ux)

- 2026-04-19 (claude) — **Docs: Manual test plan** — Created `docs/testing/TEST_PLAN.md` with 45 test cases across 7 sections: Auth (8), Projects (6), Task Submission (7), Task Edit (5), Accomplishments (5), Approval Workflow (8), Regression (6). Regression section covers all 6 bugs fixed same day. Feature-based structure with pass/fail checkboxes and Supabase verification steps. (docs/testing)

- 2026-04-19 (claude) — **Fix: 6 core-function bugs** — (1) `updateTask` edit path never set `approval_status='approved'` for auto-approved tasks (<5h saved) — task stayed stuck as 'pending' after every edit. (2) `updateSubmissionApproval` wrote to non-existent column `rejected_reason` instead of `rejection_reason` — rejection reason silently dropped. (3) `login.html` fetched profile without `email`/`is_active` and omitted `eas_user_profile_ts` cache timestamp — redundant DB round-trip on every post-login interaction. (4) `saveProject()` called `closeModal` before the async save — form data lost on failure. (5) `saveTask()` edit path closed modal before `updateTask` resolved — same data-loss issue. (6) New task submission fired two success toasts — removed duplicate. (fix/auth+db+ui)

- 2026-04-19 (claude) — **Fix: Batch recover 12 incomplete signup profiles** — Discovered systemic issue where 12 users signed up but profiles were never created (auth account exists, users/copilot_users empty). This occurred because signup_contributor RPC didn't exist before April 19. Created recover_incomplete_profile() function and batch-recovered all 12 accounts (rsadek, smagdy, bhegazy, mmabdallah, salmatrafi, asgomaa, magaber, abelllah, y.akl, aallhidan, m.habashy, aelahi). All defaulted to EPCS practice, pending status (no copilot). Improved login error message to show user's email and guide them to contact SPOC/admin. (fix/auth+db+batch-recovery)

- 2026-04-19 (claude) — **Fix: Enable profile completion on login from auth metadata** — When newly signed-up users cleared browser cache or logged in from a different device, the localStorage `eas_pending_signup` was no longer available. Login page would fail with "Account not found in the system". Updated login.html to fallback to extracting signup data from `auth.user.user_metadata` (which contains full_name, practice, skill, has_copilot stored during signup). Now profile is completed automatically from metadata when no localStorage data exists. Fixes backlog access for confirmed users. (fix/auth+login)

- 2026-04-19 (claude) — **Fix IDE data period banner** — Banner now shows the unified date range of the latest sync batch only (collapsing per-user period strings to a single "Mar 2026 to Apr 2026" label), instead of concatenating all historical periods from every past sync. (fix/ide-ui)

- 2026-04-19 (claude) — **Fix: Create missing signup_contributor RPC function** — Account creation failed at profile setup because signup_contributor() RPC was never implemented. Created SECURITY DEFINER function that creates users + copilot_users rows, handling copilot status based on has_copilot flag (access granted vs pending). Function properly uses status column (remarks was dropped April 17). Restored signup flow. (fix/db+auth)

- 2026-04-19 (claude) — **Reflect IDE data on main dashboard** — Main "Copilot Users" KPI now shows licensed count + "N active · task + IDE" sub-text (computed from practice_summary). Practice cards now include "Active Users" stat alongside hours saved/efficiency/quality. Practice detail KPI grid now includes "Licensed Users" and "Active Users" cards sourced from practice_summary.active_users (which counts task-loggers + ide_days_active > 0). (feat/dashboard+ui)

- 2026-04-19 (claude) — **IDE usage sync from Grafana dump (2026-03-22 → 2026-04-18)** — Parsed `IDE_Dump_19_Apr_26.json` (NDJSON, 3,087 daily records, 377 unique users). Created `scripts/sync_grafana_json.py` to aggregate per-user metrics and generate UPDATEs. Executed 8 batches of SQL against `copilot_users`; 32 EAS participants matched and updated across `ide_days_active`, `ide_total_interactions`, `ide_code_generations`, `ide_code_acceptances`, `ide_agent_days`, `ide_chat_days`, `ide_loc_suggested`, `ide_loc_added`, `ide_last_active_date`, `ide_data_period`. (data-sync/ide)

- 2026-04-19 (claude) — Replace all GRC projects: deleted 16 stale/duplicate entries and inserted 9 clean authoritative records (ANB CR VAT Reconciliation, ARB Counter Fraud Analytics Renewal, ARB Digital Core Banking, Experian BAU DigiCore, QNBAA BAU, SIMAH Enhancements, VAT Application Support, ANB Consumer Loans Reconciliation, ANB IT Managed Services 2025) (projects/data)

- 2026-04-18 (claude) — feat: live AI news feed — daily-refreshed from 8 RSS sources + skills.sh, filterable by topic and source, admin manual refresh (ai-news)

- 2026-04-17 (claude) — **iPhone orientation compatibility** — Fixed all iOS Safari orientation issues: (1) Added `viewport-fit=cover` to all 6 HTML pages for notch/safe-area support. (2) Replaced `100vh` with `100dvh` (dynamic viewport height) across shared CSS and inline styles — fixes the iOS address bar overlap. (3) Added `env(safe-area-inset-*)` padding to page content, headers, mobile toggle, and toast notifications — prevents content from being hidden behind the notch or home indicator. (4) Added `@media (max-height: 500px) and (orientation: landscape)` queries across all pages — compacts KPIs, charts, headers, modals, login/signup forms, and carousel for phone landscape mode. (5) Added `-webkit-text-size-adjust: 100%` to prevent font inflation on orientation change. (fix/mobile+ios)

- 2026-04-17 (claude) — **Block negative time saved + fix saved hours calc + accomplishments in scoring** — (1) Task submission now validates time saved > 0 (time_with must be less than time_without); real-time display shows negative values in red with warning. (2) Approval detail modal now computes saved hours from actual task data (time_without - time_with) instead of stale `saved_hours` in submission_approvals, fixing mismatch when tasks are edited. (3) Accomplishment effort_saved now included in champions scoring: `computeBadges()` uses combined task + accomplishment hours for Time Saver and Centurion badges; two new badges added (Innovator: 1+ accomplishment, Impact Maker: 3+ accomplishments). Leaderboard and My Practice views enriched with accomplishment data; leaderboard table shows accomplishment count column. (fix/validation+scoring+ui)

- 2026-04-17 (claude) — **New skill: dead-code-detector** — Created `.github/skills/dead-code-detector/` with `SKILL.md` and `scripts/dead_code_scan.sh`. Detects unreferenced files, unused functions/exports, orphaned imports, unused CSS classes, dead code branches, unused variables, duplicate code, and unused npm dependencies. Features: auto-fix with user confirmation, markdown report generation, memory-based incremental scanning for token optimization. (feat/skills)

- 2026-04-17 (claude) — **Fix inaccurate practice summary for SPOCs** — Rebuilt `get_practice_summary` RPC, `practice_summary` view, and `get_executive_summary` function. `licensed_users` now counts only `access granted` users (was counting all). `active_users` now computed from actual tasks table + IDE activity (`ide_days_active > 0`) instead of stale `has_logged_task` boolean. Fixed 9 users with stale `has_logged_task = true` but zero tasks. Updated `fetchInactiveMembers` in `db.js` to consider IDE activity — users with recent IDE usage no longer appear as inactive. Inactive table now shows "Last Activity" (max of task date and IDE date) with IDE days badge. Updated `001_schema.sql` to match. (fix/db+ui)

- 2026-04-17 (claude) — **Fix accomplishment approval workflow** — Accomplishments now always require mandatory two-layer review (SPOC → Admin), regardless of saved hours. Removed auto-approval for accomplishments (tasks retain existing auto-approve < 5h logic). Updated `determineApprovalRouting` to accept `submissionType` parameter, `approveSubmission` state machine to always advance accomplishments to `admin_review` after SPOC approval, and admin.html approvals UI to show submission type (Task vs Accomplishment) in both pending and history tables. Fixed broken `createSubmissionApproval` call in `updateAccomplishment` that was passing practice as wrong argument. Admin can still bypass and approve accomplishments directly at any stage. (fix/approval+ui)

- 2026-04-17 (claude) — **Unify status/remarks + enforce NOT NULL** — Dropped duplicate `remarks` column from `copilot_users`; all code now uses `status` exclusively. Normalized `Active` → `access granted` (27 rows). Added `NOT NULL` constraint on `status` with default `'pending'`. Updated `db.js`, `index.html`, `migrate.html`, and `001_schema.sql` to remove all `remarks` references. (refactor/db+code)

- 2026-04-16 (claude) — **Fix inaccurate licensed user and task counts** — Dashboard "Total Licensed Users" now counts only `access granted` users (169) instead of all copilot_users (180). Fixed stale `has_logged_task` boolean for 29 users whose tasks existed but weren't reflected. Updated `practice_summary` view, `get_licensed_tool_adoption` and `get_executive_summary` SQL functions to filter by `status = 'access granted'`. Added default `'pending'` on `copilot_users.status` column. Normalized casing (`Pending` → `pending`). (fix/db+dashboard)

- 2026-04-16 (claude) — **Featured Spotlight Banner + Global Likes System** — New auto-rotating carousel banner at the top of the dashboard showcasing top-performing tasks, accomplishments, prompts, and use cases. Hybrid selection: admin pins first, then user likes, then metric-based fallback. Global permanent like system with heart buttons on Tasks table, Accomplishment cards, Use Case cards, and Prompt Library (reuses existing `prompt_votes`). Banner auto-rotates every 5s with pause-on-hover, manual arrows, dot indicators, and full keyboard/ARIA support. Admin "Banner Settings" page for slot allocation per content type and pin/unpin management. Schema: `likes`, `featured_banner_config`, `featured_banner_pins` tables + `v_banner_candidates` view + `toggle_like` RPC. Client-side daily cache with calendar-day reset. `prefers-reduced-motion` respected. Migration: `sql/022_featured_banner_and_likes.sql`. (feat/ui+db+admin)

- 2026-04-16 (copilot) — **Auto-sync practice_spoc on role change** — When admin assigns a user as SPOC (via modal or inline dropdown), the user is automatically added to `practice_spoc` for their practice. When role changes away from SPOC, their `practice_spoc` entry is deactivated (`is_active = false`). New `syncPracticeSpoc()` in `db.js`. No manual INSERT needed. (feat/approval+db)

- 2026-04-16 (copilot) — **Multi-SPOC Approval per Practice** — Any SPOC in a practice can now approve any task for that practice (first approval wins). Multiple SPOCs can be assigned to the same practice. The "Pending With" column on the employee task status grid, main dashboard approvals page, and admin approvals page now shows actual SPOC usernames instead of a generic "SPOC" label. Schema: removed `UNIQUE(practice)` from `practice_spoc`, added `UNIQUE(practice, spoc_id)`. Updated views `employee_task_approvals`, `pending_approvals`, `spoc_approval_workload` with `pending_spoc_names` column. `fetchPendingApprovals` now matches by practice (not individual `spoc_id`). New `getSpocsForPractice()` returns all active SPOCs. Migration: `sql/021_multi_spoc_approval.sql`. (feat/approval+db+ui)

- 2026-04-16 (copilot) — **Admin Override: Approve Any Task** — Admin can now approve tasks at any approval stage (including `spoc_review`) directly, bypassing the normal SPOC→Admin flow. The state machine in `approveSubmission()` checks `userRole === 'admin'` first and sets `nextStatus = 'approved'` immediately. Dashboard `index.html` now shows Approve/Reject buttons inline for admin on all pending items, and the detail modal `canAct` check allows admin to act on any pending status. (feat/approval)

- 2026-04-16 (copilot) — **Team Lead Role** — New `team_lead` role allowing SPOCs to delegate scoped SPOC-like capabilities to practice contributors. Team leads see only their assigned members' tasks, approvals, and data. Includes: SQL migration `020_team_lead_role.sql` (table `team_lead_assignments`, RLS policies, helper functions), `auth.js` (`isTeamLead()`), `db.js` (team lead CRUD + approval scoping), `index.html` (sidebar visibility, My Practice scoping, approval access, Team Lead Management UI/modal for SPOCs, export updates), `admin.html` (role dropdowns, cleanup on role change). (feat/roles+db+ui)

- 2026-04-15 (claude) — **AI Prompt Improver (admin-only)** — New "Improve with AI" workflow on the admin Prompt Library page. Admin enters a raw English prompt; OpenAI (`gpt-4o-mini`) qualifies it into a structured prompt (ROLE/TASK/CONTEXT/INPUT/FORMAT/CONSTRAINTS), then Anthropic (`claude-sonnet-4-5`) refines it. Admin reviews 
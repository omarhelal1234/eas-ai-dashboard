# Changelog

All notable changes to the E-AI-S (EAS AI Adoption Tracker) project are recorded here.

Format: `- YYYY-MM-DD (channel) — description (scope)`
Channels: `claude` · `copilot` · `commit` · `manual`
This changelog is **append-only**. Every task, regardless of origin, must add an entry under `## [Unreleased]` per `.github/copilot-instructions.md` §4.

---

## [Unreleased]

- 2026-04-19 (claude) — **Fix: 11 bugs from manual test run BUG-01 through BUG-11** — BUG-01: Modal forms (task/accomplishment/project) now reset all fields on every fresh open. BUG-02: `saveProject` coerces empty `contract_value` to `null` instead of `0`. BUG-03: `saveTask` validates quality rating 1–5 in JS before save. BUG-04: Employee autocomplete now shows dropdown immediately after practice selection; duplicate listener accumulation eliminated. BUG-05: `updateTask`/`updateAccomplishment` handle RLS-blocked deletes by marking old approval rows `superseded`. BUG-06: `submitTaskWithApproval`/`submitAccomplishmentWithApproval` sync `approval_status` back to submission row on create. BUG-07: Bypass-approval toast reads "auto-approved (admin bypass)". BUG-08: Removed duplicate "Saving task…" toast — one toast per task save. BUG-09: Removed extra "Saving accomplishment…" and trailing toast — one toast per accomplishment save. BUG-10: `rejectSubmission` stamps `spoc_reviewed_by/name/at` when rejecting at `spoc_review` stage. BUG-11: Added `#user-display-practice` sidebar element; admin now sees home practice label. (fix/ui+db+ux)

- 2026-04-19 (claude) — **Docs: Manual test plan** — Created `docs/testing/TEST_PLAN.md` with 45 test cases across 7 sections: Auth (8), Projects (6), Task Submission (7), Task Edit (5), Accomplishments (5), Approval Workflow (8), Regression (6). Regression section covers all 6 bugs fixed same day. Feature-based structure with pass/fail checkboxes and Supabase verification steps. (docs/testing)

- 2026-04-19 (claude) — **Fix: 6 core-function bugs** — (1) `updateTask` edit path never set `approval_status='approved'` for auto-approved tasks (<5h saved) — task stayed stuck as 'pending' after every edit. (2) `updateSubmissionApproval` wrote to non-existent column `rejected_reason` instead of `rejection_reason` — rejection reason silently dropped. (3) `login.html` fetched profile without `email`/`is_active` and omitted `eas_user_profile_ts` cache timestamp — redundant DB round-trip on every post-login interaction. (4) `saveProject()` called `closeModal` before the async save — form data lost on failure. (5) `saveTask()` edit path closed modal before `updateTask` resolved — same data-loss issue. (6) New task submission fired two success toasts — removed duplicate. (fix/auth+db+ui)

- 2026-04-19 (claude) — **Fix: Batch recover 12 incomplete signup profiles** — Discovered systemic issue where 12 users signed up but profiles were never created (auth account exists, users/copilot_users empty). This occurred because signup_contributor RPC didn't exist before April 19. Created recover_incomplete_profile() function and batch-recovered all 12 accounts (rsadek, smagdy, bhegazy, mmabdallah, salmatrafi, asgomaa, magaber, abelllah, y.akl, aallhidan, m.habashy, aelahi). All defaulted to EPCS practice, pending status (no copilot). Improved login error message to show user's email and guide them to contact SPOC/admin. (fix/auth+db+batch-recovery)

- 2026-04-19 (claude) — **Fix: Enable profile completion on login from auth metadata** — When newly signed-up users cleared browser cache or logged in from a different device, the localStorage `eas_pending_signup` was no longer available. Login page would fail with "Account not found in the system". Updated login.html to fallback to extracting signup data from `auth.user.user_metadata` (which contains full_name, practice, skill, has_copilot stored during signup). Now profile is completed automatically from metadata when no localStorage data exists. Fixes backlog access for confirmed users. (fix/auth+login)

- 2026-04-19 (claude) — **Fix IDE data period banner** — Banner now shows the unified date range of the latest sync batch only (collapsing per-user period strings to a single "Mar 2026 to Apr 2026" label), instead of concatenating all historical periods from every past sync. (fix/ide-ui)

- 2026-04-19 (claude) — **Fix: Create missing signup_contributor RPC function** — Account creation failed at profile setup because signup_contributor() RPC was never implemented. Created SECURITY DEFINER function that creates users + copilot_users rows, handling copilot status based on has_copilot flag (access granted vs pending). Function properly uses status column (remarks was dropped April 17). Restored signup flow. (fix/db+auth)

- 2026-04-19 (claude) — **Reflect IDE data on main dashboard** — Main "Copilot Users" KPI now shows licensed count + "N active · task + IDE" sub-text (computed from practice_summary). Practice cards now include "Active Users" stat alongside hours saved/efficiency/quality. Practice detail KPI grid now includes "Licensed Users" and "Active Users" cards sourced from practice_summary.active_users (which counts task-loggers + ide_days_active > 0). (feat/dashboard+ui)

- 2026-04-19 (claude) — **IDE usage sync from Grafana dump (2026-03-22 → 2026-04-18)** — Parsed `IDE_Dump_19_Apr_26.json` (NDJSON, 3,087 daily records, 377 unique users). Created `scripts/sync_grafana_json.py` to aggregate per-user metrics and generate UPDATEs. Executed 8 batches of SQL against `copilot_users`; 32 EAS participants matched and updated across `ide_days_active`, `ide_total_interactions`, `ide_code_generations`, `ide_code_acceptances`, `ide_agent_days`, `ide_chat_days`, `ide_loc_suggested`, `ide_loc_added`, `ide_last_active_date`, `ide_data_period`. (data-sync/ide)

- 2026-04-19 (claude) — Replace all GRC projects: deleted 16 stale/duplicate entries and inserted 9 clean authoritative records (ANB CR VAT Reconciliation, ARB Counter Fraud Analytics Renewal, ARB Digital Core Banking, Experian BAU DigiCore, QNBAA BAU, SIMAH Enhancements, VAT Application Support, ANB Consumer Loans Reconciliation, ANB IT Managed Services 2025) (projects/data)

- 2026-04-18 (claude) — feat: live AI news feed — daily-refreshed from 8 RSS sources + skills.sh, filterable by topic and source, admin manual refresh (ai-news)

- 2026-04-17 (claude) — **iPhone orientation compatibility** — Fixed all iOS Safari orientation issues: (1) Added `viewport-fit=cover` to all 6 HTML pages for notch/safe-area support. (2) Replaced `100vh` with `100dvh` (dynamic viewport height) across shared CSS and inline styles — fixes the iOS address bar overlap. (3) Added `env(safe-area-inset-*)` padding to page content, headers, mobile toggle, and toast notifications — prevents content from being hidden behind the notch or home indicator. (4) Added `@media (max-height: 500px) and (orientation: landscape)` queries across all pages — compacts KPIs, charts, headers, modals, login/signup forms, and carousel for phone landscape mode. (5) Added `-webkit-text-size-adjust: 100%` to prevent font inflation on orientation change. (fix/mobile+ios)

- 2026-04-17 (claude) — **Block negative time saved + fix saved hours calc + accomplishments in scoring** — (1) Task submission now validates time saved > 0 (time_with must be less than time_without); real-time display shows negative values in red with warning. (2) Approval detail modal now computes saved hours from actual task data (time_without - time_with) instead of stale `saved_hours` in submission_approvals, fixing mismatch when tasks are edited. (3) Accomplishment effort_saved now included in champions scoring: `computeBadges()` uses combined task + accomplishment hours for Time Saver and Centurion badges; two new badges added (Innovator: 1+ accomplishment, Impact Maker: 3+ accomplishments). Leaderboard and My Practice views enriched with accomplishment data; leaderboard table shows accomplishment count column. (fix/validation+scoring+ui)

- 2026-04-17 (claude) — **New skill: dead-code-detector** — Created `.github/skills/dead-code-detector/` with `SKILL.md` and `scripts/dead_code_scan.sh`. Detects unreferenced files, unused functions/exports, orphaned imports, unused CSS classes, dead code branches, unused variables, duplicate code, and unused npm dependencies. Features: auto-fix with user confirmation, markdown report generation, memory-based incremental scanning for token optimization. (feat/skills)

- 2026-04-17 (claude) — **Fix inaccurate practice summary for SPOCs** — Rebuilt `get_practice_summary` RPC, `practice_summary` view, and `get_executive_summary` function. `licensed_users` now counts only `access granted` users (was counting all). `active_users` now computed from actual tasks table + IDE activity (`ide_days_active > 0`) instead of stale `has_logged_task` boolean. Fixed 9 users with stale `has_logged_task = true` but zero tasks. Updated `fetchInactiveMembers` in `db.js` to consider IDE activity — users with recent IDE usage no longer appear as inactive. Inactive table now shows "Last Activity" (max of task date and IDE date) with IDE days badge. Updated `001_schema.sql` to match. (fix/db+ui)

- 2026-04-17 (claude) — **Fix accomplishment approval workflow** — Accomplishments now always require mandatory two-layer review (SPOC → Admin), regardless of saved hours. Removed auto-approval for accomplishments (tasks retain existing auto-approve < 5h logic). Updated `determineApprovalRouting` to accept `submissionType` parameter, `approveSubmission` state machine to always advance accomplishments to `admin_review` after SPOC approval, and admin.html approvals UI to show submission type (Task vs Accomplishment) in both pending and history tables. Fixed broken `createSubmissionApproval` call in `updateAccomplishment` that was passing practice as wrong argument. Admin can still bypass and approve accomplishments directly at any stage. (fix/approval+ui)

- 2026-04-17 (claude) — **Unify status/remarks + enforce NOT NULL** — Dropped duplicate `remarks` column from `copilot_users`; all code now uses `status` exclusively. Normalized `Active` → `access granted` (27 rows). Added `NOT NULL` constraint on `status` with default `'pending'`. Updated `db.js`, `index.html`, `migrate.html`, and `001_schema.sql` to remove all `remarks` references. (refactor/db+code)

- 2026-04-16 (claude) — **Fix inaccurate licensed user and task counts** — Dashboard "Total Licensed Users" now counts only `access granted` users (169) instead of all copilot_users (180). Fixed stale `has_logged_task` boolean for 29 users whose tasks existed but weren't reflected. Updated `practice_summary` view, `get_licensed_tool_adoption` and `get_executive_summary` SQL functions to filter by `status = 'access granted'`. Added default `'pending'` on `copilot_users.status` column. Normalized casing (`Pending` → `pending`). (fix/db+dashboard)

- 2026-04-16 (claude) — **Featured Spotlight Banner + Global Likes System** — New auto-rotating carousel banner at the top of the dashboard showcasing top-performing tasks, accomplishments, prompts, and use cases. Hybrid selection: admin pins first, then user likes, then metric-based fallback. Global permanent like system with heart buttons on Tasks table, Accomplishment cards, Use Case cards, and Prompt Library (reuses existing `prompt_votes`). Banner auto-rotates every 5s with pause-on-hover, manual arrows, dot indicators, and full keyboard/ARIA support. Admin "Banner Settings" page for slot allocation per content type and pin/unpin management. Schema: `likes`, `featured_banner_config`, `featured_banner_pins` tables + `v_banner_candidates` view + `toggle_like` RPC. Client-side daily cache with calendar-day reset. `prefers-reduced-motion` respected. Migration: `sql/022_featured_banner_and_likes.sql`. (feat/ui+db+admin)

- 2026-04-16 (copilot) — **Auto-sync practice_spoc on role change** — When admin assigns a user as SPOC (via modal or inline dropdown), the user is automatically added to `practice_spoc` for their practice. When role changes away from SPOC, their `practice_spoc` entry is deactivated (`is_active = false`). New `syncPracticeSpoc()` in `db.js`. No manual INSERT needed. (feat/approval+db)

- 2026-04-16 (copilot) — **Multi-SPOC Approval per Practice** — Any SPOC in a practice can now approve any task for that practice (first approval wins). Multiple SPOCs can be assigned to the same practice. The "Pending With" column on the employee task status grid, main dashboard approvals page, and admin approvals page now shows actual SPOC usernames instead of a generic "SPOC" label. Schema: removed `UNIQUE(practice)` from `practice_spoc`, added `UNIQUE(practice, spoc_id)`. Updated views `employee_task_approvals`, `pending_approvals`, `spoc_approval_workload` with `pending_spoc_names` column. `fetchPendingApprovals` now matches by practice (not individual `spoc_id`). New `getSpocsForPractice()` returns all active SPOCs. Migration: `sql/021_multi_spoc_approval.sql`. (feat/approval+db+ui)

- 2026-04-16 (copilot) — **Admin Override: Approve Any Task** — Admin can now approve tasks at any approval stage (including `spoc_review`) directly, bypassing the normal SPOC→Admin flow. The state machine in `approveSubmission()` checks `userRole === 'admin'` first and sets `nextStatus = 'approved'` immediately. Dashboard `index.html` now shows Approve/Reject buttons inline for admin on all pending items, and the detail modal `canAct` check allows admin to act on any pending status. (feat/approval)

- 2026-04-16 (copilot) — **Team Lead Role** — New `team_lead` role allowing SPOCs to delegate scoped SPOC-like capabilities to practice contributors. Team leads see only their assigned members' tasks, approvals, and data. Includes: SQL migration `020_team_lead_role.sql` (table `team_lead_assignments`, RLS policies, helper functions), `auth.js` (`isTeamLead()`), `db.js` (team lead CRUD + approval scoping), `index.html` (sidebar visibility, My Practice scoping, approval access, Team Lead Management UI/modal for SPOCs, export updates), `admin.html` (role dropdowns, cleanup on role change). (feat/roles+db+ui)

- 2026-04-15 (claude) — **AI Prompt Improver (admin-only)** — New "Improve with AI" workflow on the admin Prompt Library page. Admin enters a raw English prompt; OpenAI (`gpt-4o-mini`) qualifies it into a structured prompt (ROLE/TASK/CONTEXT/INPUT/FORMAT/CONSTRAINTS), then Anthropic (`claude-sonnet-4-5`) refines it. Admin reviews 